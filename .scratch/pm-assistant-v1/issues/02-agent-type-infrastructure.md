# 02 — Agent 类型基础设施

**What to build:** 平台从 2 种 Agent 扩展为 4 种——新增「原型」「任务拆分」两种类型（先用占位 prompt 保证创建后对话可跑通），并配套四阶段管线指示器、各 Agent 空态文案、创建标签页弹窗的四类型展示，以及按 Agent 类型的文件自动加载规则。此 ticket 完成后，用户能创建 4 种标签页并与之对话（内容质量留待 03-06）。

**Blocked by:** None — can start immediately

**Status:** ready-for-agent

- [ ] 创建标签页弹窗展示 4 种 Agent 类型（名称 + 描述）
- [ ] 「原型」「任务拆分」tab 可创建、可对话（占位 prompt）、可重命名/关闭，行为与既有类型一致
- [ ] 管线指示器显示四阶段：需求分析 → 原型 → PRD 撰写 → 任务拆分
- [ ] 自动加载规则：原型 tab 创建时自动加载 需求分析.md（存在时）；PRD tab 创建时自动加载 需求分析.md + 原型.md（存在时）；任务拆分 tab 创建时自动加载 prd.md + 需求分析.md（存在时）
- [ ] 四种 Agent 的空态提示文案适配各自职责
- [ ] 回归：既有 2 种 Agent 行为不变，现有验证脚本通过

**备注：** 技术为主；2 个轻产品确认点已由用户在 to-tickets quiz 中确认（自动加载规则、四阶段展示文案），无需再打扰。新 Agent 类型的内部标识名由实现者定（保持与现有命名风格一致）。
